import React from "react";

const LandingDetail = () => {
  return (
    <div className="bg-green-100 h-full rounded-lg shadow-xl m-5 p-3 mt-10 colored2 text-white">
      <div>
        <p>
          Tailwind lets you conditionally apply utility classes in different
          states using variant modifiers. For example, use hover:justify-between
          to only apply the justify-between utility on hover.
        </p>
      </div>
    </div>
  );
};

export default LandingDetail;
